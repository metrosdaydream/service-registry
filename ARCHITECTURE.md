# 架构设计文档

> **设计说明**: 本架构方案（数据结构、API 规范、扩展设计）由编码者输出，工具按设计完成代码实现。

## 整体架构拓扑

```
┌─────────────────────────────────────────────────────┐
│                   浏览器 (Vue 3 SPA)                 │
│              http://localhost:3000                   │
└──────────────────────┬──────────────────────────────┘
                       │ GET /api/services (JSON)
                       ▼
┌─────────────────────────────────────────────────────┐
│              注册中心后端 (C++17)                     │
│              cpp-httplib :3000                       │
│                                                      │
│  ┌──────────────────────────────────────────┐       │
│  │  Registry (std::unordered_map + mutex)    │       │
│  │  服务名 → [{实例列表}]                     │       │
│  └──────────────────────────────────────────┘       │
└─────────┬───────────────────────────────────────────┘
          │ POST /api/register (JSON)
          ▼
┌─────────────────────────────────────────────────────┐
│           示例微服务客户端 (C++17)                    │
│           httplib Client                             │
│           启动时自动注册到注册中心                      │
└─────────────────────────────────────────────────────┘
```

## 核心流程说明

### 服务启动注册流程

1. **示例微服务启动** → 构造 httplib::Client 连接到 `localhost:3000`
2. **构造注册请求** → 将服务名、IP、端口、接口列表序列化为 JSON
3. **发送 POST /api/register** → 注册中心接收请求
4. **Registry::registerInstance()** → 检查是否已存在相同实例（同IP+同端口）
   - 已存在：更新实例信息（lastHeartbeat 刷新）
   - 不存在：插入新实例
5. **返回响应** → `{"code": 0, "message": "registered"}`

### 前端拉取数据渲染流程

1. **页面加载** → `mounted()` 触发首次 `loadServices()`
2. **fetch('/api/services')** → GET 请求到后端
3. **Registry::getAllServices()** → 返回所有已注册实例列表（加锁保证线程安全）
4. **JSON 序列化** → `allServicesToJson()` 将实例列表转为 JSON 数组
5. **前端渲染** → Vue 3 响应式数据绑定，`v-for` 渲染服务卡片
6. **按服务名分组** → 前端 computed 属性 `groupedServices` 按 `name` 字段分组
7. **自动刷新** → `setInterval` 每 5 秒自动拉取最新数据
8. **点击展开** → `toggleGroup()` 控制接口列表的展开/折叠

### 前端交互

- **首页**: 展示所有已注册服务（按服务名分组，每组一张卡片）
- **点击卡片头部**: 展开/收起该服务下的实例详情和接口列表
- **每个实例**: 显示 IP:端口 + 下属所有接口（方法、路径、名称）
- **自动刷新**: 页面每 5 秒自动轮询后端数据
- **手动刷新**: 点击右上角「立即刷新」按钮

## 数据流

```
POST /api/register 请求体:
{
  "name": "user-service",
  "ip": "127.0.0.1",
  "port": 8081,
  "interfaces": [
    {"name":"查询","path":"/api/users","method":"GET"},
    {"name":"创建","path":"/api/users","method":"POST"}
  ]
}

存储结构 (内存):
services_["user-service"] = [
  { name:"user-service", ip:"127.0.0.1", port:8081,
    status:"UP", interfaces:[...], lastHeartbeat:... }
]
```

## 扩展性设计

### 1. 服务下线

**当前状态**: 服务不会自动下线，除非收到新的注册请求覆盖。

**扩展方案**:
- 在 Registry 中添加 `unregister(name, ip, port)` 方法
- 客户端添加 `DELETE /api/unregister` 端点
- 客户端在 `SIGTERM/SIGINT` 时主动调用下线接口

### 2. 心跳检测

**当前状态**: 心跳接口已预留（`POST /api/heartbeat`），仅更新时间戳。

**扩展方案**:
- 后端启动一个后台线程，每隔 N 秒扫描所有服务实例
- 如果 `now - lastHeartbeat > timeout`，将该实例标记为 `status: "DOWN"`
- 超过最大超时次数后，自动移除该实例
- 客户端定时（如每 10 秒）发送心跳请求

### 3. 多实例集群部署

**当前状态**: 单节点，内存存储。

**扩展方案**:
- **选项一**: 共享数据库（Redis/MySQL），多个注册中心实例共享同一数据源
- **选项二**: Gossip 协议（如 Serf/Swarm），各节点间定期同步注册信息
- **选项三**: Raft 共识算法，实现强一致的分布式注册中心

### 4. 持久化

**当前状态**: 纯内存存储，重启后所有注册信息丢失。

**扩展方案**:
- 定时将 `services_` 序列化为 JSON 写入本地文件
- 启动时检查文件是否存在，加载恢复
- 使用 LevelDB/RocksDB 嵌入式数据库替代 HashMap

### 5. 接口扩展

- `GET /api/services/{name}` — 查询指定服务的所有实例
- `GET /api/services/{name}/{ip}:{port}` — 查询指定实例详情
- `PUT /api/services/{name}/{ip}:{port}` — 更新实例元数据
